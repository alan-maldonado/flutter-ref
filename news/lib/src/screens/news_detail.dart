import 'package:flutter/material.dart';

class NewsDetail extends StatelessWidget {
  Widget build(context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail'),
      ),
      body: Text('I am a news detail'),
    );
  }
}
